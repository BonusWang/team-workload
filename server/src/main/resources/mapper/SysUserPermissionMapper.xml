<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamworkload.mapper.SysUserPermissionMapper">
    <select id="selectPermissionCodesByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
        SELECT permission_code FROM sys_user_permission WHERE user_id = #{userId}
    </select>
    
    <delete id="deleteByUserId" parameterType="java.lang.Long">
        DELETE FROM sys_user_permission WHERE user_id = #{userId}
    </delete>
</mapper>